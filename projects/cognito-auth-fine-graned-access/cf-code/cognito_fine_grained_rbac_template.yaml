AWSTemplateFormatVersion: '2010-09-09'
Description: Cognito User Pool, Identity Pool, Groups, and IAM Roles for Fine-Grained API Gateway Authorization

Resources:
  # ---------------------------------------------------------
  # 1. COGNITO USER POOL & CLIENT
  # ---------------------------------------------------------
  AppUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: FineGrainedAuthPool
      AdminCreateUserConfig:
        AllowAdminCreateUserOnly: false
      AutoVerifiedAttributes:
        - email

  AppUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      ClientName: WebClient
      UserPoolId: !Ref AppUserPool
      ExplicitAuthFlows:
        - ALLOW_USER_PASSWORD_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH
        - ALLOW_USER_SRP_AUTH

  # ---------------------------------------------------------
  # 2. COGNITO IDENTITY POOL
  # ---------------------------------------------------------
  AppIdentityPool:
    Type: AWS::Cognito::IdentityPool
    Properties:
      IdentityPoolName: FineGrainedIdentityPool
      AllowUnauthenticatedIdentities: false
      CognitoIdentityProviders:
        - ClientId: !Ref AppUserPoolClient
          ProviderName: !GetAtt AppUserPool.ProviderName

  # ---------------------------------------------------------
  # 3. IAM ROLES FOR GROUPS
  # ---------------------------------------------------------
  AdminIAMRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: ApiAdminRole
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Federated: cognito-identity.amazonaws.com
            Action: sts:AssumeRoleWithWebIdentity
            Condition:
              StringEquals:
                "cognito-identity.amazonaws.com:aud": !Ref AppIdentityPool
              "ForAnyValue:StringLike":
                "cognito-identity.amazonaws.com:amr": authenticated
      Policies:
        - PolicyName: AdminApiInvokeAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: execute-api:Invoke
                # Admin has full access to all API GW paths/methods
                Resource: "arn:aws:execute-api:*:*:*/*/*/*" 

  DevIAMRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: ApiDevRole
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Federated: cognito-identity.amazonaws.com
            Action: sts:AssumeRoleWithWebIdentity
            Condition:
              StringEquals:
                "cognito-identity.amazonaws.com:aud": !Ref AppIdentityPool
              "ForAnyValue:StringLike":
                "cognito-identity.amazonaws.com:amr": authenticated
      Policies:
        - PolicyName: DevApiInvokeAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: execute-api:Invoke
                # Dev is restricted to specific paths (e.g., /dev/*)
                Resource: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:*/*/GET/dev/*"

  # ---------------------------------------------------------
  # 4. USER POOL GROUPS (Attaching Roles)
  # ---------------------------------------------------------
  AdminUserGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: Admin
      Precedence: 1
      RoleArn: !GetAtt AdminIAMRole.Arn
      UserPoolId: !Ref AppUserPool

  DevUserGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: Dev
      Precedence: 2
      RoleArn: !GetAtt DevIAMRole.Arn
      UserPoolId: !Ref AppUserPool

  # ---------------------------------------------------------
  # 5. IDENTITY POOL ROLE ATTACHMENT (The RBAC Magic)
  # ---------------------------------------------------------
  # This tells the Identity Pool to respect the Group Roles assigned in the token
  IdentityPoolRoleMapping:
    Type: AWS::Cognito::IdentityPoolRoleAttachment
    Properties:
      IdentityPoolId: !Ref AppIdentityPool
      Roles:
        # Fallback role if a user has no group
        authenticated: !GetAtt DevIAMRole.Arn 
      RoleMappings:
        UserPoolMapping:
          Type: Token
          AmbiguousRoleResolution: Deny
          IdentityProvider: !Sub "cognito-idp.${AWS::Region}.amazonaws.com/${AppUserPool}:${AppUserPoolClient}"

Outputs:
  UserPoolId:
    Value: !Ref AppUserPool
  AppClientId:
    Value: !Ref AppUserPoolClient
  IdentityPoolId:
    Value: !Ref AppIdentityPool